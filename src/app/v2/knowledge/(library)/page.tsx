/**
 * Knowledge Library Dashboard (default route)
 */

import { LibraryContent } from '@/lib/v2/library-routes';

export default function KnowledgeLibraryDashboard() {
  return <LibraryContent activeTab="dashboard" />;
}
